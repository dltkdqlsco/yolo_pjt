[project]
name = "yolo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sangbin", email = "dltkdqls7899@naver.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
yolo = "yolo:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
